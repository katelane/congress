<header>
  <%= link_to(image_tag("header.png", :alt => "Logo"), root_path) %>
  <% if current_user %>
    <div class="header-blurb">Signed in as <%= current_user.name %><br>
    <%= link_to "Logout", sessions_path, method: :delete%></div>
  <% end %>
  <div style="clear:both;"></div>
</header>
<hr>
