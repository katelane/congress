<h2>Search Results:</h2>
<a href="https://twitter.com/share" class="twitter-share-button" data-text="Congress talks about:" data-count="none" data-hashtags="justtalkinaboutcongress">Tweet</a>
<script>!function(d,s,id){var js,fjs=d.getElementsByTagName(s)[0],p=/^http:/.test(d.location)?'http':'https';if(!d.getElementById(id)){js=d.createElement(s);js.id=id;js.src=p+'://platform.twitter.com/widgets.js';fjs.parentNode.insertBefore(js,fjs);}}(document, 'script', 'twitter-wjs');</script>

<% @sunlights.each do |x| %>
<div class="sunlights">
  <div class="sunlights-left">
    Title: <%= x.title %><br>
    <% if x.speaker_first == nil %>
      Speaker: <em>N/A</em><br>
    <% else %>
      Speaker: <%= x.speaker_first %> <%= x.speaker_last %> (<%= x.speaker_state %>-<%= x.speaker_party %>)<br>
    <% end %>
    Mentioned in the <%= x.chamber %> on <%= Date.parse(x.date).strftime("%m/%d/%Y") %>.<br>
  </div>
  <div class="sunlights-right">
    Excerpt: ... <%= x.speaking.pop %> ...<br><br>
    <%= link_to "Read full text", x.origin_url, :target => '_blank' %>
  </div>
  <div style="clear:both;"></div>
</div>
<% end %>

<div class="sunlights-buttons">
  <%= link_to "Back to top", "#", :class => "sunlights-top" %>
</div>
<div style="clear:both;"></div>

<br>
